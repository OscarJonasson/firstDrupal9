/**
 * @file
 * Custom_auto_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customAutoTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
